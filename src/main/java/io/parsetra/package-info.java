/**
 * ParseTra: human-readable duration and data size parsing and formatting for Java.
 * <p>
 * Entry points: {@link io.parsetra.duration.Durations} for durations,
 * {@link io.parsetra.size.Sizes} for byte sizes.
 */
package io.parsetra;
