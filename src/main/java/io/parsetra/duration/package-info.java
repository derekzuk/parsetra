/**
 * Duration parsing and formatting: parse strings like "2h 30m" or "1.5d"
 * into {@link java.time.Duration} and format back to human-readable strings.
 *
 * @see io.parsetra.duration.Durations
 */
package io.parsetra.duration;
