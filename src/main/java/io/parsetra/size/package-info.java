/**
 * Data size parsing and formatting: parse strings like "10MB" or "2GiB"
 * into byte counts and format byte counts to human-readable strings.
 *
 * @see io.parsetra.size.Sizes
 */
package io.parsetra.size;
